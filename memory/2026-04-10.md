# 2026-04-10

- User preference: after simulation runs or other meaningful repo changes, commit and push automatically without asking; large diffs are fine.
- User preference: treat this repo not only as project source, but also as a backup of working state, including agent/runtime-support files and memory.
- User preference: routine housekeeping should be delegated to a cheaper MiniMax worker when possible to save tokens and limits.
- Runtime architecture clarification: persistent agents (`rundm`, `runplayerXX`) should communicate via direct `sessions_send`; avoid `sessions_spawn` for normal run execution.
- Runtime workspace rule: agent workspaces must stay separate; only `rundm` needs a direct link from its workspace `campaign/` into the shared runtime `campaign/`. Players should not get runtime write linkage.
- Important runtime contract rule: `rundm <-> runplayerXX` communication should use strict JSON packets/replies, not free prose.
- Repo source-of-truth was added for runtime bootstraps under `runtime_agents/`, with sync script `scripts/sync_runtime_agent_bootstraps.sh`; intended workflow is edit bootstrap in repo first, then sync to live agent workspaces.
- Bootstrap design rule: keep `rundm` bootstrap self-contained in `SOUL.md`; avoid hard dependency on extra template files for core behavior.
